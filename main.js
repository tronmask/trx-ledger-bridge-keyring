"use strict";
import "babel-polyfill";
import LedgerBridge from "./ledger-bridge";

(async () => {
  const bridge = new LedgerBridge();
})();
console.log("TronMask < = > Ledger Bridge initialized!");
